CREATE DATABASE `web_account_db` CHARACTER SET utf8 COLLATE utf8_general_ci;

USE web_account_db;
DROP TABLE IF EXISTS amounts;
CREATE TABLE amounts (
ID INT NOT NULL AUTO_INCREMENT
, category_id INT NOT NULL
, name VARCHAR(40) NOT NULL
, price DECIMAL(6, 2) NOT NULL DEFAULT 0
, date DATE NOT NULL
, details VARCHAR(256)
, PRIMARY KEY (id)
);

USE web_account_db;
DROP TABLE IF EXISTS categories;
CREATE TABLE categories (
ID INT NOT NULL AUTO_INCREMENT
, parent_id INT
, name VARCHAR(40) NOT NULL
, details VARCHAR(256)
, type TINYINT(1) DEFAULT 0
, PRIMARY KEY (id)
);

INSERT INTO categories (parent_id, name, details, type) VALUES (0, 'name1', 'details1', 0);
INSERT INTO categories (parent_id, name, details, type) VALUES (1, 'name2', 'details2', 1);

INSERT INTO amounts (category_id, name, price, date, details)
    VALUES (1, 'name1', 100, CURDATE(), "details1");
INSERT INTO amounts (category_id, name, price, date, details)
    VALUES (2, 'name2', 10000, CURDATE(), "details2");
INSERT INTO amounts (category_id, name, price, date, details)
    VALUES (1, 'name3', 100, CURDATE(), "details3");

http://javastudy.ru/spring-data-jpa/spring-data-jpa-helloworld/
http://javastudy.ru/interview/jee-spring-questions-answers/

USE web_account_db;
DROP TABLE IF EXISTS amounts;
CREATE TABLE amounts (
ID INT NOT NULL AUTO_INCREMENT
, category_id INT
, name VARCHAR(40)
, price DECIMAL(10, 2) DEFAULT 0
, date DATE
, details VARCHAR(256)
, PRIMARY KEY (id)
);

USE web_account_db;
DROP TABLE IF EXISTS categories;
CREATE TABLE categories (
ID INT NOT NULL AUTO_INCREMENT
, parent_id INT
, name VARCHAR(40)
, details VARCHAR(256)
, type TINYINT(1) DEFAULT 0
, PRIMARY KEY (id)
);